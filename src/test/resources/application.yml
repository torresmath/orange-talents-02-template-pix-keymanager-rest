micronaut:
  applicattion:
    name: key-manager-rest-test
  server:
    port: 8081
  http:
    services:
      local-test:
        urls:
          - http://localhost:8081

api.version: v1

jackson:
  date-format: yyyy-MM-dd HH:mm:ss
  serializationInclusion: ALWAYS